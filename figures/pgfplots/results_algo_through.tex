
\documentclass[tikz,margin=0pt,dvipsnames,x11names,rgb]{standalone}

\input{../stdl_preamble.tex}

\begin{document}
\begin{tikzpicture}
  \begin{axis}
    [
      width=8 cm,
      height=8 cm,
      ybar = .7,
      bar width = 24pt,
      ymin = 0,
      symbolic x coords={p8, p16, p64},
      enlarge x limits = .5,
      xtick = data,
      ytick = {0,...,5},
      major tick length = 0ex,
      xticklabels = {8, 16, 64},
    %   yticklabels = {{}},
      xmajorgrids = true,
      ymajorgrids = true,
      xlabel = {\phantom{()}\pd{}\phantom{()}},
      ylabel = {Débit d'informations (kb/s)},
      nodes near coords,
      nodes near coords style = {black},
      legend pos=north west,
    ]
    \addplot coordinates {
        (p8,   5)
        (p16, 2.5)
        (p64, 0.6)
      };

    \addplot coordinates {
        (p64, 4)
      };
  \end{axis}
\end{tikzpicture}
\end{document}
