\documentclass[tikz,margin=0pt,dvipsnames,rgb]{standalone}

\input{../stdl_preamble.tex}

\begin{document}
\begin{tikzpicture}[
    thick
  ]
  \scriptsize
  \begin{axis}[
      % title={Detection Score (function of $\Delta$ and $\omega$)},
      xlabel={$\Delta$},
      ylabel={Score},
      width=10cm,
      height=8cm,
      xmin=1, xmax=1280,
      ymin=20, ymax=128,
      xtick={128, 384, 640, 896, 1152},
      xticklabels={{-512}, {-256}, {0}, {+256}, {+512}},
      ytick={40, 60, 80, 100, 120},
      yticklabels={{20}, {25}, {30}, {35}, {40}},
      legend pos=north east,
      xmajorgrids=true,
      yminorgrids=true,
      ymajorgrids=true,
      grid style=dashed,
      axis x line = bottom,
      axis y line = left,
      at={(0, 0)}
    ]

    \addplot[color=blue!60!cyan,]
    table[x=x, y=0, col sep=comma]{\fromRoot{data/scores_det.dat}};
    \addplot[color=red!40!orange,]
    table[x=x, y=pi_2, col sep=comma]{\fromRoot{data/scores_det.dat}};
    \addplot[color=yellow!80!red,]
    table[x=x, y=pi, col sep=comma]{\fromRoot{data/scores_det.dat}};
    \addplot[color=red!50!blue,]
    table[x=x, y=2pi, col sep=comma]{\fromRoot{data/scores_det.dat}};
    \draw (1,60) -- (1280, 60)
    node [pos=.11, above] {$U_0$};

    \legend{$\omega = 0$,$\omega = \frac{\pi}{2}$,$\omega = \pi$,$\omega = 2\pi$}
  \end{axis}

\end{tikzpicture}
\end{document}